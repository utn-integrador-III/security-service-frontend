import React from 'react';

const Roles: React.FC = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold">Roles Management</h1>
      {/* La gestión de roles irá aquí */}
    </div>
  );
};

export default Roles;
